version: "3.9"
services:
  scraper:
    build: ./scraper
    volumes:
      - ./scraper:/app
    command: python main.py

  api:
    build: ./api
    ports:
      - "5000:5000"
    volumes:
      - ./api:/app

